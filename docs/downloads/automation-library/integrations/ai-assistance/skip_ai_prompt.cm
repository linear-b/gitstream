# -*- mode: yaml -*-

manifest:
  version: 1.0

automations:
  skip_ai_assistance_prompt:
    # Skip posting the AI assistance question if a relevant label is already applied
    if:
      - {{ pr.labels | match(pattern='🤖 ai-*') | some }}
    run:
      - action: noop
