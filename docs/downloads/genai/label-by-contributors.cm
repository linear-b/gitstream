# -*- mode: yaml -*-

manifest:
version: 1.0

automations:
#
label_genai:
  # label PRs by a given set of authors with "copilot"
   if:
    - {{ pr.author | match(list=genai_contribs) | some  }}
  # Apply a safe change label, approve the PR and explain why in a comment.
  run:
    - action: add-label@v1
      args:
        label: '🚀 Copilot'
        color: '#387B95'

genai_contribs:
- contributor_handle1
- contributor_handle2
- contributor_handle3
