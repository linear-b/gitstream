# -*- mode: yaml -*-

manifest:
  version: 1.0

automations:
  is_gitstream_user:
    if:
      - {{ pr.author | isGitstreamUser }}
    run:
      - action: add-comment@v1
        args:
          comment: {{ pr.author }} is a gitStream user.